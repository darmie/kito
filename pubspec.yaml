name: kito
description: A powerful, fine-grained reactive animation library for Flutter inspired by anime.js
version: 0.1.0
homepage: https://github.com/yourusername/kito

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.10.0

  # Reactive primitives
  kito_reactive:
    path: packages/kito_reactive

  # State machine library
  kito_fsm:
    path: packages/kito_fsm

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
